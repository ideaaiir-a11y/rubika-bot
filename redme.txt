✅ سیستم اتوماسیون تلگرام آماده شد!

## 📁 فایل‌های ساخته شده

| فایل | توضیح |
|------|-------|
| [`content.csv`](content.csv) | فایل ورودی با محتوای آماده |
| [`bot.py`](bot.py) | اسکریپت اصلی پایتون |
| [`.github/workflows/auto.yml`](.github/workflows/auto.yml) | اجرای خودکار هر ۳۰ دقیقه |
| [`requirements.txt`](requirements.txt) | وابستگی‌ها |

## 🚀 راه‌اندازی (۵ مرحله)

### ۱. آپلود به گیت‌هاب
```bash
git init
git add .
git commit -m "Initial commit"
gh repo create telegram-bot --public --source=. --push
```

### ۲. تنظیم Secrets در گیت‌هاب
به Settings → Secrets and variables → Actions بروید و این متغیرها را اضافه کنید:

| Secret | دریافت از |
|--------|----------|
| `BOT_TOKEN` | @BotFather در تلگرام (ساخت ربات جدید) |
| `CHAT_ID` | آی‌دی کانال تلگرام |
| `PEXELS_KEY` | https://www.pexels.com/api (رایگان) |

### ۳. تنظیم کانال تلگرام
- ربات ساخته شده را به کانال اضافه کنید
- ربات را به عنوان Admin با دسترسی ارسال پست تنظیم کنید

### ۴. تست دستی
```bash
python bot.py
```

### ۵. فعال‌سازی خودکار
در گیت‌هاب: Actions → Telegram Content Automation → Enable

## 📊 نتیجه
- ✅ ۱۰۰% رایگان (گیت‌هاب اکشنز + Pexels API)
- ✅ کار ۲۴/۷ بدون توقف
- ✅ هر ۳۰ دقیقه یک پست جدید
- ✅ تصویر + ترجمه فارسی + هشتگ + لینک تبلیغات