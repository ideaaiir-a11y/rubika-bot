# Rubika Content Automation Bot 🚀

این پروژه یک بات اتوماسیون برای تولید و ارسال محتوای خودکار به کانال‌های روبیکا است.  
بات با استفاده از **GitHub Actions** به صورت رایگان و 24/7 اجرا می‌شود.

---

## ✨ قابلیت‌ها
- ترجمه عناوین انگلیسی به فارسی
- تشخیص دسته‌بندی محتوا (شهر، غذا، ساحل، سفر و …)
- جستجوی تصویر مرتبط از Pexels API
- ساخت کپشن همراه با توضیحات، هشتگ‌ها و ایموجی‌ها
- ارسال خودکار پست به کانال روبیکا
- زمان‌بندی اجرای خودکار با GitHub Actions

---

## ⚙️ تنظیمات مورد نیاز
برای اجرای صحیح بات باید متغیرهای محیطی زیر را در **GitHub Secrets** تعریف کنید:

| نام متغیر | توضیح |
|-----------|-------|
| `BOT_TOKEN` | توکن ربات روبیکا |
| `CHAT_ID`  | شناسه کانال یا گروه مقصد |
| `PEXELS_KEY` | کلید API سرویس Pexels (اختیاری) |

---

## 📂 ساختار پروژه
- `main.py` → کد اصلی بات
- `content.csv` → فایل ورودی شامل لیست محتوا (ستون‌های title, source, likes)
- `.github/workflows/automation.yml` → فایل تنظیمات GitHub Actions

---

## 📝 نمونه دستورات بات
start       شروع کار بات و نمایش پیام خوش‌آمدگویی
help        نمایش لیست دستورات و توضیحات آن‌ها
post        ارسال یک پست جدید به کانال یا گروه
stats       نمایش آمار پست‌ها (لایک، نظر، اشتراک‌گذاری)
translate   ترجمه عنوان‌ها از انگلیسی به فارسی
image       جستجو و دریافت تصویر مرتبط از Pexels
schedule    زمان‌بندی ارسال خودکار پست‌ها

Code

---

## 🚀 اجرای خودکار
این پروژه با GitHub Actions به صورت زمان‌بندی‌شده اجرا می‌شود.  
پس از هر بار Push یا طبق کرون‌جاب تعریف‌شده، بات محتوای جدید را پردازش و به کانال ارسال می‌کند.

---

## 📌 نکات مهم
- مطمئن شوید ربات به کانال اضافه شده و دسترسی ارسال پیام دارد.  
- فایل `content.csv` باید در ریپو وجود داشته باشد.  
- اگر کلید Pexels تعریف نشده باشد، از تصویر پیش‌فرض استفاده می‌شود.  

---

## 📜 License
این پروژه تحت لایسنس MIT منتشر شده است.
